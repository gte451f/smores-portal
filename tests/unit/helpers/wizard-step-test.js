import { wizardStep } from '../../../helpers/wizard-step';
import { module, test } from 'qunit';

module('Unit | Helper | wizard step');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = wizardStep(42);
  assert.ok(result);
});
